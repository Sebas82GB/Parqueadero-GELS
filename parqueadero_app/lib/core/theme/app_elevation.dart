/// Elevaciones — mayormente formaliza los valores por defecto de Material 3
/// que ya estaban implícitos (nadie en el proyecto pasaba `elevation:` antes
/// de este token).
class AppElevation {
  const AppElevation._();

  static const flat = 0.0;
  static const low = 1.0;
  static const raised = 3.0;
}
